import { StyleSheet } from "react-native";

export default StyleSheet.create({
    bodyText: {
        // fontFamily: 'open-sans',
        // color:'red'
        fontSize: 20,
        fontWeight: "bold",
        margin:20
    }
});